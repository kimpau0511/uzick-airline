const $$=(s,c=document)=>[...c.querySelectorAll(s)];const $=(s,c=document)=>c.querySelector(s);
const nav=$('.nav'),menu=$('.menu-btn');menu?.addEventListener('click',()=>{const open=nav.classList.toggle('open');menu.setAttribute('aria-expanded',open)});$$('.nav a').forEach(a=>{if(a.getAttribute('href')===location.pathname.split('/').pop()||(!location.pathname.split('/').pop()&&a.getAttribute('href')==='index.html'))a.setAttribute('aria-current','page')});
const modal=$('#modal');let lastFocus;function openModal(html){lastFocus=document.activeElement;$('#modalContent').innerHTML=html;modal.classList.add('open');modal.setAttribute('aria-hidden','false');$('.modal-close').focus()}function closeModal(){modal?.classList.remove('open');modal?.setAttribute('aria-hidden','true');lastFocus?.focus()}$('.modal-close')?.addEventListener('click',closeModal);modal?.addEventListener('click',e=>{if(e.target===modal)closeModal()});document.addEventListener('keydown',e=>{if(e.key==='Escape')closeModal()});
function toast(msg){const t=$('.toast');if(!t)return;t.textContent=msg;t.classList.add('show');setTimeout(()=>t.classList.remove('show'),2200)}
function clocks(){const zones=[['clock-seoul','Asia/Seoul'],['clock-newyork','America/New_York'],['clock-paris','Europe/Paris']];zones.forEach(([id,z])=>{const el=$('#'+id);if(el)el.textContent=new Intl.DateTimeFormat('ko-KR',{timeZone:z,hour:'2-digit',minute:'2-digit',second:'2-digit',hour12:false}).format(new Date())})}clocks();setInterval(clocks,1000);
async function weather(){const el=$('#weatherState');if(!el)return;try{const r=await fetch('https://api.open-meteo.com/v1/forecast?latitude=37.5665&longitude=126.9780&current=temperature_2m,weather_code,wind_speed_10m&timezone=Asia%2FSeoul');if(!r.ok)throw 0;const d=await r.json(),c=d.current;const names={0:'맑음',1:'대체로 맑음',2:'구름 조금',3:'흐림',45:'안개',51:'이슬비',61:'비',71:'눈',80:'소나기',95:'뇌우'};el.innerHTML=`<span class="weather-temp">${Math.round(c.temperature_2m)}°</span><span><strong>서울 · ${names[c.weather_code]||'현재 날씨'}</strong><br><small>바람 ${Math.round(c.wind_speed_10m)} km/h · 실시간 관측</small></span>`}catch{el.innerHTML='<span>날씨를 불러오지 못했습니다.<br><button class="btn small" onclick="weather()">다시 시도</button></span>'}}weather();
window.UZ={openModal,closeModal,toast};
