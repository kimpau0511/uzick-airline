let filter='전체';function renderGallery(){const items=$$('.gallery-item');items.forEach(i=>i.hidden=!(filter==='전체'||i.dataset.category===filter))}$$('.gallery-filter').forEach(b=>b.onclick=()=>{$$('.gallery-filter').forEach(x=>x.classList.remove('active'));b.classList.add('active');filter=b.dataset.filter;renderGallery()});$$('.gallery-item').forEach(b=>b.onclick=()=>{const img=b.dataset.image,title=$('strong',b).textContent,desc=$('span',b).textContent;UZ.openModal(`<img class="modal-image" src="${img}" alt="${title}"><h2>${title}</h2><p>${desc}</p>`) });
